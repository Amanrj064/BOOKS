<header>
  <h1>🌟 Neon Book Library</h1>
  <% if (session && session.isAdmin) { %>
    <a class="admin-btn" href="/admin">➕ Add Book</a>
    <a class="admin-btn" href="/logout">Logout</a>
  <% } else { %>
    <a class="admin-btn" href="/login">Admin Login</a>
  <% } %>
</header>
